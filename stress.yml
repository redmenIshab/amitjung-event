config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 1900
      maxVusers: 1900
scenarios:
  - name: "Full booking pipeline"
    flow:
      - post:
          url: "/api/stress/book"
          headers:
            Content-Type: application/json
            x-stress-key: "test-secret-123"
          json:
            eventId: "cmqkw4qmu0001kfes4yjn8yz9"
            participantId: "cmqkvjeam0000kfesndhmqmd1"
            attendees:
              - name: "User {{ $arrivalCount }}"
                email: "user{{ $arrivalCount }}@example.com"
                category: "GENERAL"
            amounts:
              totalAmount: 1000
              discountAmount: 0
              discountPercentage: 0
              finalAmount: 1000

