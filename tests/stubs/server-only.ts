// Stub for the `server-only` package. See the alias note in vitest.config.ts:
// the real module throws on import outside a react-server condition, so tests
// that exercise server modules substitute this no-op.
export {}
